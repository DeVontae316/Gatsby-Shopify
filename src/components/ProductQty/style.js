import styled from "styled-components";
import { ProductQty } from ".";

export const ProductQtyWrapper = styled.div`
  margin-top: 10px;
  > strong {
    display: block;
    margin-bottom: 20px;
  }
  > form {
    display: flex;
    flex-direction: row;
  }
`;
